# Edge model placement

Place the reviewed `fire_smoke_detector.tflite` and generated `model_manifest.json` here for deployment. Model binaries are ignored by Git. The service must verify the SHA-256, input tensor rank/dimensions/type, output types/indexes, and label contract before normal inference.

No trained model was supplied with this repository, so hardware inference is blocked rather than simulated in QNX hardware mode.

