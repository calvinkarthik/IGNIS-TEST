# Incident engine

The pure deterministic engine is implemented in `common/src/incident.cpp` and covered by portable tests. The service process is the QNX integration host for detection IPC, local incident JSONL, evidence freeze notifications, alarm notifications, and stream notifications.

