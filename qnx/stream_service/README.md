# Stream service

The service establishes the production authenticated TCP connection, sends network-order packets, and reconnects with bounded exponential backoff while local services continue. The current target seam sends health and is ready to drain incident/event IPC ahead of replaceable JPEG frames. The simulator exercises the complete packet classes until shared-memory/JPEG target integration is bound.

