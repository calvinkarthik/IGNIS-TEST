# Alarm service

The default null adapter records alarm intent without driving hardware. Enable a buzzer or LED only after the Pi's supported GPIO API and safe electrical interface are verified. Alarm output is optional and must never imply certification.

