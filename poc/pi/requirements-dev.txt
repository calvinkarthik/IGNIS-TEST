# These are for a Python-capable development/target image. QNX packages must match its ABI.
numpy==2.2.6
opencv-python-headless==4.12.0.88
# Install the QNX/ARM-compatible TensorFlow Lite runtime supplied for the target.

